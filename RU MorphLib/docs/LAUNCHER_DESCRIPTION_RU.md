# Launcher Description RU

Текущий текст в `mod_info.json`:

```text
RU MorphLib - развивающаяся модульная библиотека русской морфологии для локализации Starsector и различных модификаций. Она даёт переводчикам более широкие и гибкие инструменты для продвинутого перевода: токены рода и падежей для NPC, обращения к игроку, плюрализацию и формы чисел, ресурсы и memory-числа, динамические суффиксы и Public Quantity API.
```

Примечание: поле `author` в `mod_info.json` уже содержит `Lasplagas`, поэтому описание не дублирует авторство.
